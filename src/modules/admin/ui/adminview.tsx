export const dynamic = "force-dynamic";

import React from "react";
import MenuManager from "./menu-list";

export default function AdminView() {
  return (
    <div>
      <MenuManager />
    </div>
  );
}
